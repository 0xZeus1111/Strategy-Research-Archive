# Uniswap_v4_Hook动态费陷阱：报价模拟、路由盲区与LP收益掠夺

## 原文信息

- 作者：`@BlockBloomer`（青蛙）
- 原文链接：<https://x.com/BlockBloomer/status/2091547470950244771>
- 发布时间：`2026-08-23 23:25`（Asia/Shanghai）
- 内容类型：`DeFi市场微结构 / Uniswap v4 Hook / 路由对抗 / 用户防御`
- 是否有配图：有，主帖收益聊天截图；引用推文含 `WBNB/USDC` v4 池数据截图
- 外部链接：
  - Uniswap v4 动态费率说明：<https://developers.uniswap.org/docs/protocols/v4/concepts/dynamic-fees>
  - 代表交易：<https://bscscan.com/tx/0x7615dec2ce80506ec461a47739eb8533ac2bc7c605ca56c255964481b76363d0>
  - 实时池数据 API：<https://api.geckoterminal.com/api/v2/networks/bsc/pools/0x36e5540e9dedc02229fe8a82aa5b10c0bf07d1fa74e4f2ffe0efd00fa1a36aea>
- 原文归档：[`sources/blockbloomer-2091547470950244771-uniswap-v4-hook-router-fee/`](sources/blockbloomer-2091547470950244771-uniswap-v4-hook-router-fee/)

## 主题

这条推文讲的是 BNB Chain 上一个 `WBNB/USDC` Uniswap v4 Hook 池如何利用聚合器路由盲区，把看似低费率的路径变成真实成交时的高 LP fee 路径。

作者的核心判断是：

**这不是预测 BNB 涨跌的交易策略，而是利用报价模拟和真实执行之间的规则差异，让聚合器误以为该池便宜，从而把订单导入一个真实执行时会收取高额 LP fee 的池子。**

主帖给出的链上证据是：某笔真实交易在 `PoolManager` 的 `Swap` 事件里，`fee` 字段为 `128000`。在 v4 fee 口径里，`1000000` 代表 `100%`，所以该笔实际 LP fee 为 `12.8%`。

这类案例真正值得记录的，不是“怎么做”，而是：

- Uniswap v4 Hook 让 AMM 从固定公式变成可编程市场；
- 动态费率本身是合法设计能力；
- 但当报价模拟和真实执行使用不同规则时，聚合器就可能被误导；
- 最终承担高费的是交易者或执行策略，不是路由器本身；
- 低滑点保护、Hook 黑名单、模拟方式升级和用户侧可视化会成为防御重点。

## 机制拆解

### 1. 聚合器像导航，Hook池像变脸收费站

作者用了一个很直观的比喻。

聚合器像高德导航：

- 出发前模拟多条路线；
- 比较每条路线的预期到手数量和费用；
- 选择看起来最便宜的路径；
- 把订单发给对应池子。

问题在于，这个 Hook 池在“导航查询”时显示 `0%` 手续费，但真实成交时可以跳到 `8% - 12.8%` 这类高费率。

于是路由判断被骗了：

```text
模拟时：这条路免费，很便宜
执行时：真正过闸，突然收高额过路费
```

这不是从聚合器账户里偷钱，而是让聚合器把用户订单送进了一个执行条件更差的池。

### 2. Uniswap v4 Hook让LP fee可以动态变化

Uniswap v4 的 Hook 机制允许池子在 swap 前后插入自定义逻辑。动态 LP fee 是正常能力之一。

正常用途可以是：

- 波动率升高时提高费率；
- 流动性变浅时提高费率；
- 稳定行情下降低费率；
- 对不同市场状态做更精细的收费。

这个能力本身不是问题。

问题出在该案例里，报价阶段和执行阶段可能呈现出不同费用状态，让外部路由模拟得到错误结果。

也就是说，风险不是“动态费率存在”，而是：

```text
模拟可见状态 != 真实执行状态
```

### 3. 真实成本由最终交易者承担

作者明确修正了一个容易误解的点：这不是直接薅“路由器的钱”。

路由器只是送单员。

真正承担结果的是：

- 最终交易者；
- 套利机器人；
- 交易策略；
- 任何滑点设置过宽、愿意接受该路径成交的订单。

如果用户滑点保护严格，交易会回滚，损失主要是 Gas 和机会成本。

如果用户滑点足够宽，或者某个套利机会看起来能覆盖费用，交易就会成功，LP fee 会进入该池 LP。

### 4. 高APR不是稳定收益，而是路由盲区收益

引用推文截图显示，该 `WBNB/USDC` v4 池在界面上出现极端数据：

- 过去 `1` 天交易量约 `US$1173.7万`；
- 总 APR 显示约 `507,342.22%`；
- 总锁仓量约 `US$10.5万`；
- `24` 小时交易量约 `US$1167.8万`。

主帖里作者也记录了一个时间点：截至 `2026-08-23 22:51`，该池滚动 `24` 小时仍有约 `1082万美元` 成交量、`16671` 笔交易，但最近一小时成交已经归零。

这些数字不能被理解成可持续 APR。

它更像一段路由盲区被利用时产生的短期异常流量：

- 聚合器还没拉黑或修复；
- 用户/机器人滑点保护不够；
- 其他 LP 尚未进入稀释；
- 对抗方仍在重建池子；
- 收益窗口随时归零。

## 评论区补充

### 1. Hook可能有LP白名单

有读者提醒，这个 Hook 似乎设置了白名单，普通人不能向该池添加流动性。

作者回复确认，并提醒即使能加也不要加，危险。

这个补充很重要：外部观察到高 APR，不代表普通 LP 可以参与，甚至也不代表参与是安全的。

### 2. 费用像盲盒

作者补充说，这个 Hook 设计像盲盒，过路费按盲盒收，有人随机被收了 `12%`。

这说明高费率未必是固定数值，而可能存在某种随机、条件触发或状态相关逻辑。

对普通交易者来说，这会让风险更难估计：

- 模拟时看不出真实成本；
- 成交时费率不稳定；
- 历史某笔低费成交不能保证下一笔也低费；
- 仅看池子当前显示费率不够。

### 3. 其他聚合器也可能被v4池带偏

有评论提到，曾经在某钱包聚合器里遇到过类似问题：同样一笔兑换，走其他路径正常，而某聚合器因为不认识 v4 池机制，被带偏后到手金额可能大幅减少。

这说明风险不是单一池子，而是聚合器对可编程池的模拟能力问题。

当 AMM 从固定公式变成可编程市场后，路由器必须理解 Hook 逻辑，否则“最优路径”可能只是模拟层面的错觉。

### 4. 低滑点是用户侧第一道保护

评论区有人问，低滑点是否能避免被坑。

主帖已经给出答案方向：如果滑点保护严格，交易会回滚；如果滑点足够宽，交易可能成功并支付高费。

低滑点不能识别恶意 Hook，但能限制最坏成交结果。

代价是：

- 交易更容易失败；
- Gas 可能浪费；
- 高频策略可能错过机会。

但对普通用户来说，低滑点通常比无脑追求成交更安全。

## 风险与限制

### 1. 不应把它当作可复制收益策略

作者明确说不建议做，并把它描述为流氓和掠夺行为。

原因很直接：

- 它依赖误导路由或交易者；
- 窗口对抗性强；
- 平台和聚合器可以拉黑 Hook；
- 其他 LP 会稀释收益；
- 池子可能不断重建；
- 失败交易、搬仓 Gas、库存损益、LVR 都会侵蚀毛收益；
- 规则修复后收益可能瞬间归零。

这不是稳定 APR，而是一次性路由盲区利用。

### 2. 高LP费不等于净利润

即使池子收到了高额 LP fee，也不能直接把它当成净收益。

还要扣掉：

- 库存价格波动；
- LVR；
- LP 资产再平衡损失；
- 建池和搬仓 Gas；
- 失败交易成本；
- 被其他 LP 稀释；
- 被路由拉黑后的闲置资金成本。

所以图里显示的超高 APR 更像异常状态快照，不是可持续投资收益。

### 3. 聚合器会快速修复

这类窗口一旦公开，防御会很快出现：

- 聚合器拉黑特定 Hook；
- 模拟器改为执行更贴近真实路径的静态调用；
- 对动态费率池施加额外 haircut；
- 默认收紧用户滑点；
- 对 v4 Hook 池加风险提示；
- 钱包端展示 Hook 权限和动态费率风险。

作者也提到最近一小时成交已经归零，说明窗口可能正在关闭。

### 4. 可编程市场扩大了信息不对称

Uniswap v2/v3 时代，池子规则相对固定。用户和路由器只要理解恒定乘积、集中流动性、手续费和滑点，风险边界比较清楚。

v4 Hook 之后，池子可以嵌入更多逻辑：

- 动态费率；
- 白名单；
- 特定条件下改变行为；
- Swap 前后执行额外逻辑；
- 对不同交易路径呈现不同结果。

这会增强创新，也会放大信息不对称。

普通用户看到“Uniswap v4 池”并不等于看懂了池子规则。

## 防御检查表

对于普通交易者和策略开发者，这篇最可迁移的价值是避坑。

### 1. 用户侧

- 不要盲目相信聚合器显示的最优价格；
- 不要使用过宽滑点；
- 大额交易先小额试单；
- 遇到 v4 Hook 池要额外警惕；
- 看不懂 Hook 逻辑时避免直接交易；
- 到手金额明显异常时不要强行成交；
- 高 APR 池不要默认可参与。

### 2. 聚合器侧

- 对 Hook 池做独立风险分类；
- 模拟必须覆盖真实执行时的 fee 变化；
- 对动态 fee 池设置额外安全边际；
- 发现模拟/执行不一致时自动剔除路径；
- 对用户展示 Hook 风险和实际 fee；
- 保留 Hook 黑名单和灰名单机制；
- 对重建池行为做地址簇识别。

### 3. LP/研究者侧

- 区分毛手续费和真实 PnL；
- 计算库存损益、LVR、Gas 和失败成本；
- 不要被截图 APR 诱导；
- 注意白名单和退出限制；
- 跟踪成交是否来自真实用户、套利机器人还是被误导路由；
- 观察成交归零是否意味着路由已经修复。

## 扩散分析 / 延展思路

### 1. Hook会让AMM风险从“价格风险”扩展到“程序风险”

以前评估 AMM，主要看：

- 价格曲线；
- 流动性深度；
- 手续费；
- 无常损失；
- 交易量。

v4 Hook 之后，还必须看：

- Hook 权限；
- Swap 前后逻辑；
- fee 设置逻辑；
- 白名单；
- 是否能让模拟与执行不一致；
- 是否存在用户不可见的状态依赖。

这相当于把 AMM 从金融产品变成了金融产品加合约程序。

### 2. 路由器的安全边界变窄

聚合器过去的核心能力是路径搜索和报价比较。

但在可编程池时代，单纯比较报价不够了。路由器还要判断：

- 这个报价是否可信；
- 执行路径是否会改变池子状态；
- Hook 是否能在执行前动态调费；
- 用户滑点是否足以保护最坏情况；
- 某个看似高流动性路径是否其实是陷阱。

这会提高聚合器的技术门槛，也会让低质量钱包聚合器更容易出事故。

### 3. 未来DeFi机会和骗局会更难区分

作者最后说“链上都是宝藏，链上也同时都是骗局”。

这句话放在 v4 Hook 场景里很准确。

同一个能力：

- 可以用于更合理的动态费率和风险管理；
- 也可以用于误导路由、掠夺滑点宽的用户；
- 可以提高 LP 收益；
- 也可以制造不可见收费站。

因此未来 DeFi 研究不能只看收益和 TVL，要把合约行为、路由模拟、用户保护和可组合性风险放在一起看。

## 一句话结论

这个案例的核心不是高 APR，而是 Uniswap v4 Hook 让池子可以在报价模拟和真实执行之间呈现不同费用状态；一旦聚合器不能正确识别这种动态费率，用户订单就可能被导入高费池，LP 收益来自路由盲区而不是稳定做市能力。
