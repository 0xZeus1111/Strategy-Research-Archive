# 高频做市脚本结构：Basis Alpha、GLFT动态点差与库存风控

## 原文信息

- 作者：`@fxu_starr`
- 原文链接：`https://x.com/fxu_starr/status/2076071222568448494`
- 发布时间：`2026-07-12 06:28`
- 内容类型：`技术知识 / 工程实现 / 高频做市框架`
- 原文归档：`sources/fxu-starr-2076071222568448494-hft-market-making-script/`
- 配图 / 附件：主帖无图片、无外链、无 `X Article`；评论区有一条关于 `GLFT + 泊松过程` 的技术补充。

## 主题

这条内容讲的是：**一个高频做市脚本应该如何从“市场数据 -> 预测中间价 -> 动态报价 -> 订单管理 -> 风控停机 -> 监控日志”拆成可实现的工程模块。**

它不是在给一个完整可复制的策略参数，也不是在讲普通方向交易。真正有价值的是它把做市系统拆成了几个层：

- 数据层：拿到 trades、orderbook、BBO 和跨所行情；
- 预测层：用 `Basis`、`OBI`、`OFI` 等构造 `alpha`，生成 `forecast midprice`；
- 报价层：围绕预测价格铺单，并用 `GLFT` 估计动态 spread；
- 库存层：根据名义持仓价值倾斜半差价，避免库存堆积；
- 执行层：订单生成、刷新、ID 映射；
- 风控层：识别交易强度异常和有毒订单流，触发平仓停机；
- 运维层：日志和状态监控。

所以这篇更像一份高频做市机器人设计草图。

## 问题定义

做市脚本的目标不是“预测下一根 K 线涨跌”，而是在连续报价中解决三个问题：

1. **报价中心在哪里？**

   不能机械围绕当前 `midprice` 报价，因为当前盘口可能已经落后于真实价格。需要用 `alpha` 把当前中间价修正成 `forecast midprice`。

2. **报价离中心多远？**

   spread 太窄，会被有信息的订单流打穿；spread 太宽，则没成交、没收益。需要根据交易强度、盘口深度和成交概率动态调整。

3. **库存歪了怎么办？**

   做市最危险的不是某一笔亏损，而是持续收到同方向坏库存。库存偏斜后，报价必须倾斜，必要时扩大 spread 或停机。

作者的结构化脚本，就是围绕这三个问题展开。

## 实现思路 / 系统设计

### 1. 先写 Label，再让代码对齐结构

作者提到的第一个方法论不是模型，而是开发流程：

先把脚本结构写成注释 `Label`，每一步规划清楚，再按模块实现。

这对高频策略尤其重要。因为做市系统不是一个单函数策略，而是一组并发状态机：

- 行情在变；
- 订单在挂和撤；
- 成交在回报；
- 库存在更新；
- 风控在监控；
- 日志在记录；
- 多交易所数据可能不同步。

如果没有先把结构写清楚，很容易写成一团循环和回调，后面无法定位问题。

比较可落地的模块划分是：

```text
MarketData
  -> FeatureBucket
  -> AlphaModel
  -> ForecastMid
  -> QuoteModel
  -> InventoryManager
  -> OrderManager
  -> RiskEngine
  -> MonitorLogger
```

这条链条清楚后，才适合用 `AI` 对照每个 `Label` 补代码。

### 2. 市场数据层：不要只看一个 midprice

作者要求获取的数据包括：

- `trades`
- 多档 `orderbook`
- `BBO`
- 多交易所行情

这里的关键是：做市系统不能只看当前最优买卖价。

原因很直接：

- `trades` 告诉你真实成交方向和交易强度；
- 多档 `orderbook` 告诉你流动性分布和可被打穿的深度；
- `BBO` 方便快速算当前盘口中心；
- 跨所数据用于识别 lead-lag 和基差 `Basis`；
- 深度变化能帮助判断订单流是否有毒。

如果只围绕单一交易所的 `midprice` 对称挂单，系统看起来很中性，但实际可能一直在落后市场。

### 3. FeatureBucket：把高频噪音变成可用特征

作者说要开 `bucket` 或 `bin` 来计算 `alpha` 和交易强度。

这一步的本质是把逐笔高频数据聚合成可用状态。

常见输入可以包括：

- 固定时间窗口内的成交量；
- 主动买入 / 主动卖出不平衡；
- 订单簿买卖盘深度差；
- `OBI`；
- `OFI`；
- 跨所基差；
- 盘口撤单速度；
- 价格到达某一深度的频率；
- 自己订单的成交率。

关键不是指标越多越好，而是每个特征都要服务于报价：

- 它是让报价中心上移 / 下移？
- 还是让 spread 变宽 / 变窄？
- 还是让系统停止报价？

没有明确动作映射的特征，会变成调参噪音。

### 4. AlphaModel：用 Basis 修正 forecast midprice

作者目前验证有效的版本，是用跨所基差 `Basis` 的线性回归作为 `alpha`。

可以理解成：

```text
forecast_mid = current_mid + f(Basis)
```

其中 `Basis` 不是单纯价差，而是一个跨交易所相对定价信号。

如果另一个交易所的价格领先，而本交易所盘口暂时没跟上，做市商不能还围绕旧的 `midprice` 报价。否则他挂出去的被动单会变成别人的免费期权。

更合理的做法是：

- 外部市场更强，本地 forecast mid 上移；
- 外部市场更弱，本地 forecast mid 下移；
- 基差信号强，报价中心更快跟随；
- 基差信号弱，回到本地盘口中心。

作者用线性调整，优点是简单、稳定、可解释。缺点也明显：一旦关系非线性或 regime 变化，线性 `alpha` 可能反而把报价推向错误方向。

### 5. QuoteModel：围绕 forecast mid，而不是 raw mid 铺单

有了 `forecast midprice` 后，下一步才是决定挂单：

- 离中心多远；
- 挂几层；
- 每层多少量；
- 多久刷新；
- 是否向某一侧倾斜。

这一步非常容易被低估。很多人以为做市就是：

```text
bid = mid - spread
ask = mid + spread
```

但真实做市至少要变成：

```text
bid = forecast_mid - adjusted_half_spread
ask = forecast_mid + adjusted_half_spread
```

`forecast_mid` 解决价格中心问题，`adjusted_half_spread` 解决风险补偿问题。

如果中心错了，spread 再漂亮也没用；如果 spread 错了，中心对了也会被毒流打。

### 6. GLFT：用交易强度和到达深度估计动态 spread

作者使用 `GLFT` 模型来做库存管理、动态 spread 和铺单层级。

在这条推文的语境里，`GLFT` 的核心作用不是展示复杂公式，而是把做市报价从固定 spread 改成跟随市场状态变化：

- 交易越活跃，订单到达越快；
- 价格越容易打到深层报价，做市商越要重新估计风险；
- 深度不同，成交概率和逆向选择风险不同；
- spread 应该随着交易强度、深度到达概率和库存状态变化。

作者提到用前 `50-70` 档数据做参数拟合。这说明它不是只看一档盘口，而是试图估计价格到达不同深度的概率结构。

更直白地说：

**GLFT 在这里负责回答：我离中间价挂多远，既能成交，又不会太容易被打穿？**

### 7. InventoryManager：用半差价倾斜管理库存

作者说，如果现在有仓位，就用仓位个数计算名义持仓价值，再加权到铺单半差价上，让半差价倾斜。

这一步是做市系统的生死线。

假设当前库存偏多：

- 系统应该降低继续买入的吸引力；
- 同时提高卖出库存的概率；
- 具体表现为 bid 侧更保守、ask 侧更积极。

假设当前库存偏空，则反过来。

这和已有仓库里“做市商库存报价系统”的逻辑一致：做市商不是永远对称报价，而是会根据库存偏斜调整 reservation price 和 spread。

但作者也提醒：线性仓位惩罚可能影响 `alpha` 有效性，并导致逆向成交。

这句话很关键。

如果库存惩罚太强，系统可能明明看到了有效 `alpha`，却因为急着修库存而挂出错误价格；如果库存惩罚太弱，系统会让 `alpha` 不断把自己推向单边库存。

所以这不是“加个库存项就完事”，而是要处理 `alpha` 和库存惩罚之间的冲突。

### 8. OrderManager：订单 ID 映射必须简单可靠

作者的小技巧是用 `price tick` 作为唯一 `orderID`，避免同一价格重复下单。

这个技巧背后的工程问题是：高频做市里订单状态很容易乱。

常见问题包括：

- 本地以为撤单成功，交易所实际没撤；
- 本地以为订单还在，交易所已经成交；
- 同一个价格重复挂单；
- 多层报价刷新后 ID 对不上；
- 网络延迟导致状态回报乱序；
- 成交回报和撤单回报交叉到达。

用 `price tick` 做唯一映射，可以降低同价重复下单的概率。但它也有边界：

- 同一价格如果需要多笔不同 size 的订单，会不够用；
- 如果交易所原生 order ID 和本地 ID 映射不一致，仍要维护状态表；
- 如果跨账户或跨交易所，同一个 tick 不能全局唯一。

所以它是一个实用简化，而不是完整订单系统设计。

### 9. RiskEngine：有毒订单流不是继续调参能解决的

作者把风控和 `kill-switch` 单独列出来，这点很对。

停机条件包括：

- 交易强度异常；
- 有毒订单流；
- 市价平仓；
- `sleep 8hr`。

这说明作者并不把做市系统理解成“永远在线赚 spread”的机器。

做市收益来自提供流动性，但有些市场状态下，你提供的不是流动性，而是在给有信息的交易者退出。

这时继续缩 spread、加层级、调库存惩罚都不一定有用。正确动作可能是：

- 撤单；
- 降低报价频率；
- 扩大 spread；
- 平掉库存；
- 停机观察。

`sleep 8hr` 虽然看起来粗暴，但它表达了一个重要原则：当模型无法解释当前订单流时，退出比优化参数更好。

### 10. MonitorLogger：没有日志，就没有策略

作者最后提到监控日志。

对高频做市来说，日志不是运维附属品，而是策略本体的一部分。

至少需要记录：

- 行情延迟；
- 订单刷新频率；
- 下单、撤单、成交回报；
- 每层订单成交率；
- 当前库存；
- 当前 forecast mid；
- 当前 spread；
- 当前 `alpha` 分量；
- 风控触发原因；
- 每个交易所连接状态；
- 实际成交价与理论报价偏差。

如果没有这些日志，策略亏钱时只能猜：

- 是 `alpha` 错？
- 是 spread 太窄？
- 是库存惩罚太弱？
- 是订单状态错？
- 是延迟太高？
- 是某个交易所数据异常？

猜不出来，就无法迭代。

## 关键技术点 / 难点

### 1. 成交概率模型不是预测市场，而是校准自己的报价

作者说，成交概率模型不是用来预测成交，而是根据自己挂单成交比率进行价差微调，维持持续报价和计划比例的 `order filled rate`。

这个区别很重要。

它不是问：

```text
市场下一秒会不会成交？
```

而是问：

```text
我挂在这个深度、这个 size、这个 spread 上，实际成交率是否符合目标？
```

如果成交率过高，可能说明 spread 太窄，或者你正在接有毒流；如果成交率过低，可能说明报价太远，策略只是挂着好看。

所以成交概率模型更像报价反馈控制器。

### 2. `alpha` 和库存控制天然冲突

做市系统里最常见的冲突是：

- `alpha` 告诉你价格要上移，应该少卖、多买；
- 库存告诉你已经太多多头，应该少买、多卖。

这两个信号经常互相打架。

如果完全听 `alpha`，库存可能爆；如果完全听库存，可能逆着真实价格修仓。

作者提到线性库存惩罚会影响 `alpha` 有效性，本质就是这个问题。

更成熟的处理方式通常不是简单相加，而是要按市场状态改变权重：

- 正常市场：允许 `alpha` 主导；
- 库存接近上限：库存惩罚增强；
- 有毒流出现：撤单或停机；
- 高波动状态：降低 size、扩大 spread；
- 对冲腿充足：库存容忍度提高。

### 3. 跨所 Basis Alpha 的有效性依赖延迟结构

跨所基差能作为 `alpha`，前提是某个市场确实领先另一个市场。

如果两个市场已经完全同步，基差只会变成噪音。更糟的是，如果你拿到的数据延迟比别人更高，所谓 `Basis Alpha` 可能只是“你看到的旧价格”。

因此必须持续监控：

- 数据时间戳；
- 本地接收延迟；
- 交易所撮合延迟；
- 下单到达延迟；
- 基差信号半衰期；
- 信号出现后本地盘口追随速度。

没有延迟监控，跨所 alpha 很容易变成伪 alpha。

### 4. `GLFT + 泊松过程` 的评论补充

评论区提到在 `GLFT` 中加入泊松过程来处理库存。

这个补充可以理解为：订单到达和成交强度本身是随机的，不能只用静态平均值或简单线性惩罚。

如果用泊松过程近似订单到达，就可以更自然地描述：

- 某一价格深度的成交到达概率；
- 单位时间内订单到达次数；
- 库存累积速度；
- 极端成交簇带来的库存跳变；
- 当前 spread 下预期成交率是否异常。

这和作者主帖里的“交易强度”“成交概率模型”“动态 spread”是同一个方向。

### 5. `price tick` 作为 order ID 是简化，不是完整架构

这个 trick 很实用，但只适合比较简单的单价格单订单结构。

如果策略进入更复杂阶段，仍需要完整订单状态机：

```text
intended_order
  -> submitted
  -> accepted
  -> partially_filled
  -> filled / canceled / rejected
```

并且要能处理：

- 交易所回报乱序；
- 部分成交；
- 撤单失败；
- 网络重连；
- 本地状态和交易所状态不一致；
- 重启后的订单恢复。

如果没有订单状态机，高频策略很容易死在工程细节上，而不是死在模型上。

## 风险与限制

### 1. 这不是“拿 AI 写完就能跑”的系统

作者提到可以让 `AI` 对照 `Label` 逐条实现。这个思路适合提高开发效率，但不能替代策略验证。

高频做市的危险在于：

- 代码看起来对；
- 回测看起来对；
- 小仓位看起来对；
- 一到真实延迟、滑点、撤单失败、行情跳变时就完全变形。

AI 可以帮你填模块，但不能替你证明订单状态、延迟、风控和资金安全。

### 2. 线性模型容易被 regime shift 打穿

作者当前用 `Basis` 线性回归作为 `alpha`。

这适合先跑通系统，但它的风险是：

- 基差关系可能只在某些时段有效；
- 高波动时线性关系断裂；
- 交易所维护、断连、风控、插针会污染样本；
- 一个交易所从领先变成滞后时，模型方向会错。

所以 `alpha` 必须持续做在线评估，不能只靠历史拟合。

### 3. 动态 spread 不能修复错误的 fair price

如果 `forecast midprice` 错了，spread 再动态也只是围绕错误中心报价。

这类错误尤其容易出现在：

- 跨所数据延迟；
- 盘口被刷量；
- 某交易所价格异常；
- 大户扫单后短暂失真；
- 指数源异常；
- 本地时钟不同步。

因此 fair price 的数据质量比 spread 模型更底层。

### 4. 有毒订单流下，成交越多可能亏得越快

做市系统最容易出现账面幻觉：成交率高、返佣多、spread 收入看起来不错，但库存方向持续变坏。

有毒订单流的特点是：

- 只成交你错误的一侧；
- 成交后价格继续朝不利方向走；
- 库存越修越歪；
- 你以为自己在做市，实际是在接刀。

所以 kill-switch 是必要模块，不是保守配置。

### 5. 高频系统的真实门槛在运维

策略写出来只是开始。

长期运行还要处理：

- 交易所 API 更新；
- WebSocket 断线；
- 订单回报丢失；
- 服务器时间同步；
- 风控参数热更新；
- 日志存储和告警；
- 仓位和余额异常；
- 交易所限频；
- 重启恢复。

这些工程问题解决不了，模型再精致也不能上规模。

## 扩散分析 / 延展思路

### 1. 可以把这条推文改写成做市机器人最小可行架构

最小版本不需要一开始就追求全功能。

可以先做：

```text
单交易所行情
  -> BBO / orderbook
  -> 简单 forecast mid
  -> 固定 spread 双边挂单
  -> 库存上限
  -> 撤单刷新
  -> 日志
  -> 手动停机
```

确认订单状态和风控可靠后，再加：

- 跨所 `Basis alpha`；
- `OBI / OFI`；
- 动态 spread；
- GLFT 参数拟合；
- 成交概率反馈；
- 自动 kill-switch。

顺序很重要。先把系统活下来，再追求模型复杂度。

### 2. 与 Polymarket 做市材料可以互相印证

仓库里已有 `Polymarket` 双边做市复盘，核心教训是：报价决定毛利，库存决定亏损。

这条推文给的是更偏高频盘口的工程版本：

- `Polymarket` 文档强调库存配平和逆向选择；
- 这条强调 `forecast mid`、`GLFT spread`、订单刷新和成交率；
- 两者都把 kill-switch 和库存管理放在比报价本身更重要的位置。

这说明不管是预测市场还是加密盘口做市，最后都绕不开同一个问题：

**做市不是挂单赚钱，而是在不确定订单流里控制库存和信息劣势。**

### 3. 可以进一步拆成四个研究方向

这条推文可以延展成四个专题：

1. `Basis Alpha`：跨所价格领先滞后、半衰期、延迟监控。
2. `OBI / OFI`：订单簿不平衡和订单流不平衡如何预测短期 midprice。
3. `GLFT / AS` 做市模型：库存惩罚、动态 spread、到达强度。
4. 风控工程：有毒订单流检测、订单状态机、kill-switch、监控告警。

每个方向都可以独立成一篇，不应该混成一个“万能做市脚本”。

### 4. 更保守的实盘原则

如果要从学习走向实盘，应该先把限制写死：

- 单品种；
- 小资金；
- 单交易所；
- 只挂一层；
- 不隔夜；
- 库存硬上限；
- 断线立即撤单；
- 无法确认订单状态立即停机；
- 日志可复盘后再加复杂模型。

这比一开始就上跨所、动态 spread、多层挂单更现实。

## 一句话结论

**这条推文真正有价值的不是某个 `Basis + GLFT` 参数，而是把高频做市拆成可实现、可监控、可停机的模块：先用数据和 alpha 修正报价中心，再用动态 spread 与库存惩罚控制成交质量，最后用订单状态机和 kill-switch 保证系统不要在有毒订单流里活活成交到死。**
