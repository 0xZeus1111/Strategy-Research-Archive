# 有毒Uniswap_v4_Hook：gas分支、quote执行错配与聚合器防御

## 原文信息

- 作者：`@brucexu_eth`（brucexu.eth）
- 原文链接：<https://x.com/brucexu_eth/status/2091888306049220613?s=20>
- 发布时间：`2026-08-24 22:00`（Asia/Shanghai）
- 内容类型：`DeFi市场微结构 / 聚合器路由安全 / Uniswap v4 Hook / 可编程AMM防御`
- 是否有配图：主帖无配图
- 引用推文：`@BlockBloomer` 对同一 `WBNB/USDC` v4 Hook 池的用户侧解释，已归档为 [`Uniswap_v4_Hook动态费陷阱：报价模拟、路由盲区与LP收益掠夺.md`](Uniswap_v4_Hook动态费陷阱：报价模拟、路由盲区与LP收益掠夺.md)
- 原文归档：[`sources/brucexu-2091888306049220613-uniswap-v4-toxic-hook-gas-fee/`](sources/brucexu-2091888306049220613-uniswap-v4-toxic-hook-gas-fee/)

## 主题

这条推文是在前一条 `BlockBloomer` 案例基础上，从聚合器开发者视角解释：为什么一个带恶意 Hook 的 Uniswap v4 动态费率池，能在报价模拟时看起来很便宜，却在真实执行时收取高额 LP fee。

作者的核心判断是：

**这类问题不是普通滑点或价格波动，而是 quote 和 execution 看到的规则不一致。聚合器用高 gas 的 `eth_call` 做报价时可能落入低费分支，真实交易用更接近实际的 gas envelope 执行时却落入高费分支，最终用户收到的数量变少，但交易仍然可以成功。**

这篇内容的价值不在于“怎么复现攻击”，而在于把可编程 AMM 对路由器、钱包和用户的安全边界讲清楚。

## 机制拆解

### 1. 用户看到的是交易成功，实际问题是到账变少

这类攻击最隐蔽的地方，是它不一定让交易失败。

表层结果是：

- 用户发起 swap；
- 聚合器找到一条看似最优路径；
- 路径中某一步进入 Uniswap v4 动态费率池；
- 报价阶段显示低费率，甚至接近 `0%`；
- 真实执行时 LP fee 被调高；
- 最终输出仍高于 `amountOutMin`，交易成功；
- 用户到账数量低于报价预期。

所以它比“直接失败”更难被普通用户感知。失败交易至少会暴露异常；成功但少到账，容易被误认为普通滑点、税费币、价格变动或多跳损耗。

### 2. 聚合器为什么会被模拟骗过

聚合器报价系统通常会先用 `eth_call` 模拟不同路径，比较各路径的预期输出。

为了避免模拟因为 gas 不够而失败，报价系统可能给 `eth_call` 一个很高的 gas ceiling。这个做法在普通池子里通常只是工程上的保守设置，但在可编程 Hook 池里，它会变成可被识别的环境特征。

作者调查到，该恶意 Hook 会读取执行时的 gas 环境。于是同样的 `beforeSwap` 输入，在不同 gas envelope 下可能返回不同 fee override：

```text
高 gas 模拟：落入低费分支，看起来适合路由
真实交易：gas limit 更接近实际执行，落入高费分支
```

作者给出的调查时配置显示：

- `gas = 1,000,000` 时，fee 约为 `10%`；
- `gas ≈ 16,000,000` 时，fee 约为 `0.5%`；
- `gas = 30,000,000` 时，fee 为 `0%`。

这些数值不是全局常量，而是作者调查当时的合约配置。历史交易中的 `12.8%` 则来自 `PoolManager` 的 `Swap` event。

关键点是：

```text
同一笔 swap 的报价环境和执行环境不一致
```

只要 Hook 能识别这种差异，就能让聚合器在报价阶段看到一套规则，在真实执行阶段撞上另一套规则。

### 3. Uniswap v4 Hook 本身不是问题，问题是可编程逻辑被用来识别报价环境

Uniswap v4 允许每个池绑定 Hook。Hook 可以在 swap 前后运行自定义逻辑。动态费率池还允许 `beforeSwap` Hook 为单笔交易覆盖 LP fee。

正常情况下，这个能力可以用于：

- 波动率上升时调高手续费；
- 流动性变浅时提高补偿；
- 稳定行情下降低交易成本；
- 按市场状态设计更细的费率曲线。

风险在于，恶意 Hook 可以把“这次调用像不像聚合器报价模拟”也放进费率逻辑里。

因此，`dynamic fee` 本身不是恶意信号；真正危险的是：

- 未验证 Hook；
- 行为依赖执行环境；
- 报价和执行返回不同费用；
- 聚合器没有对这种差异做二次验证；
- 用户的 `amountOutMin` 留出了足够空间。

### 4. `0x800000` 不是 800% 手续费

作者特别澄清了一个容易误读的链上字段。

`PoolKey` 里的 `0x800000` 是 dynamic-fee flag，表示该池的 LP fee 可以被动态覆盖，不代表 `800%` 手续费。

真实费率要看每笔 `Swap` event 里的 `fee` 字段。

本案代表交易里出现的 `fee = 128000`，按 Uniswap v4 的百万分比单位换算：

```text
128000 / 1000000 = 12.8%
```

这就是“12.8% LP fee”的链上来源。

## 代表交易复盘

作者给出的代表交易是：

<https://bscscan.com/tx/0x7615dec2ce80506ec461a47739eb8533ac2bc7c605ca56c255964481b76363d0>

这笔交易不是单纯的一跳 swap，而是一条 Relay 顶层流程。source-side swap 由 `0x BnbSettler` 执行。

作者整理的调用层级可以压缩为：

```text
ERC-4337 EntryPoint
  ↓
Relay v3 Router / ApprovalProxy / Depository
  ↓
0x BnbSettler
  ├─ FOMO action：USDT -> WBNB
  └─ Uniswap v4 action：WBNB -> USDC
      ↓
      目标动态费池 + 恶意 Hook
```

关键不是 FOMO 这一步。FOMO 只完成了 `USDT -> WBNB`。

真正出问题的是后续 `0x BnbSettler` 单独进入 Uniswap v4 池，把 `WBNB` 换成 `USDC` 的步骤。

作者给出的数值大致是：

- 进入问题步骤前，价值约 `5,926.9 USDT`；
- FOMO 输出约 `8.536 WBNB`；
- 目标 v4 pool 执行后得到约 `5,169.5 USDC`；
- 稳定币价值差约 `757.4` 美元；
- 亏损比例约 `12.779%`，基本吻合 `12.8%` LP fee；
- 该交易的 `minAmountOut` 约 `4,742.5 USDC`；
- 实际输出仍比 minimum 高约 `9%`，所以交易正常成功。

这说明 `amountOutMin` 没有失效。它严格检查了最终输出底线。

问题在于，这个底线本身离费前路线价值太远，给中间异常高费留出了空间。

作者也没有把责任简单归到用户手动设置。他强调，仅凭链上数据无法确认约 `20%` 的 buffer 来自哪里。它可能来自用户设置，也可能来自 Relay、0x 或上层集成针对 meme、tax、低流动性、多跳路线自动加入的保护空间。

## 反编译信息怎么理解

作者提到，该 Hook 没有 verified source，也没有 Sourcify metadata。调查者取得 runtime bytecode 后，通过反编译和 live `eth_call` 验证恢复了部分控制流。

这里需要把重点放在防御信息，而不是实现细节。

可以确认的高层行为是：

- Hook 的 `beforeSwap` 只接受 `PoolManager` 调用；
- 合约会读取池子配置和执行环境；
- fee tier 的选择与 gas 环境有关；
- 同一输入在不同 gas envelope 下会返回不同 fee override；
- 历史 `Swap` event 确实记录了 `12.8%` fee。

不能过度确认的是：

- 反编译里的函数名；
- 变量名；
- 完整控制流；
- 攻击者选择具体阈值和费率的真实动机。

所以这份材料应被当成链上行为复盘和防御案例，而不是 verified source 审计报告。

## `amountOutMin` 的边界

`amountOutMin` 是最终输出检查，不是路径诚实性检查。

它能做的是：

- 如果最终到账低于底线，交易回滚；
- 如果最终到账高于底线，交易通过；
- 帮用户限制最大可接受损失。

它不能做的是：

- 逐跳检查中间池 fee 是否和 quote 一致；
- 判断 Hook 是否临时改变了规则；
- 区分普通滑点和恶意动态费用；
- 保证聚合器模拟结果与真实执行一致。

所以“滑点设低一点”仍然是普通用户最直接的保护，但不是完整解决方案。

评论区有人问：聚合器支持 JIT，或者用户把 slippage 设成 `10%`，能否杜绝问题。

作者回复的重点是：不能完全杜绝。很低的 `minOut` 仍可能让部分流量走进毒池，只是损失会小一点。

这也是为什么聚合器侧必须做环境一致性检查，而不能把全部安全责任推给用户滑点。

## 聚合器侧防御框架

这条推文最值得迁移的是聚合器防御框架。

### 1. 高 gas `eth_call` 不能再单独作为可信报价

过去，高 gas 模拟通常是为了提高报价稳定性。

在可编程池里，它可能反而制造盲区。

更稳妥的做法是：

- 对同一 calldata 做高 gas 模拟；
- 再用接近真实交易 gas limit 的环境模拟；
- 比较 fee、output、路径和池事件预期；
- 如果结果显著不同，直接拒绝或降权该路径。

核心不是“模拟一次能不能成功”，而是“不同执行环境下规则是否一致”。

### 2. dynamic-fee pool 和 unknown Hook 要单独分层

Uniswap v4 之后，池子不能只按深度、价格、手续费、历史成交量来排序。

还要识别：

- 是否为 dynamic-fee pool；
- Hook 是否 verified；
- Hook 是否有历史异常 fee；
- quote fee 和 receipt fee 是否一致；
- 同一 Pool ID 是否频繁被重建或迁移；
- Hook 是否可能依赖 `gasleft`、`tx.origin`、余额、区块 gas limit 等执行环境。

评论区也有人提醒，从聚合器视角看，toxic pool 的博弈很不对称。攻击者不只可以看 gas，还可能看其他上下文变量来动态调费。

这意味着防御不能只修一个条件，而要把“环境敏感型 Hook”作为一类风险。

### 3. 执行后要做 receipt 级监控

即使交易成功，也不能直接认为路线健康。

聚合器应当在执行后核对：

- quote 时选择的 Pool ID；
- receipt 里真实经过的 Pool ID；
- Hook 地址；
- LP fee；
- 实际 output；
- 与 quote 的偏差；
- 是否出现低费报价、高费执行。

一旦发现 quote 和 execution 不一致，应当进入告警、降权、灰名单或黑名单流程。

这也是评论区那句总结的含义：`quote execution mismatch is the real attack surface`。

## 用户侧防御

普通用户很难分析 Hook 字节码，也不可能每次手工复核路由。

但仍有几件事可以降低损失：

- 大额交易不要使用过宽滑点；
- 交易前确认钱包展示的 minimum 是否真的能接受；
- 对 meme、tax、低流动性、多跳路线额外谨慎；
- 如果可见路径包含 Uniswap v4 Hook 池，要把它当成更高风险路径；
- 大额兑换可以先小额测试，但不能把一次小额成功当作后续一定安全；
- 到账明显低于报价预期时，不要简单归因为“滑点正常”。

普通用户的核心保护仍是 `amountOutMin`，但它只是损失底线，不是路由诚信证明。

## 与上一条 BlockBloomer 分析的关系

上一条 `BlockBloomer` 分析回答的是：

```text
为什么一个看似高 APR 的 WBNB/USDC v4 Hook 池，本质上是路由盲区收益，而不是稳定 LP 策略
```

这条 `brucexu_eth` 补上的是：

```text
聚合器为什么会把订单送进去，以及如何从工程侧识别 quote 和 execution 的错配
```

两篇合在一起，完整框架是：

- 用户侧看到的是高 APR、交易成功、到账偏少；
- 池子侧利用的是 Uniswap v4 Hook 的动态 fee override；
- 聚合器侧暴露的是高 gas 模拟和真实执行之间的环境差异；
- 链上证据落在 `Swap` event 的 fee 字段和实际 output；
- 防御重点从“调低滑点”扩展到“检查 Hook 行为是否环境敏感”。

## 风险与限制

### 1. 不能把反编译结果当作 verified source

作者明确说明，Hook 没有公开 verified source。反编译只能帮助理解行为，不能替代源码审计。

因此，文档里的技术判断应以链上可验证行为为准：

- 不同 gas envelope 下返回不同 fee；
- 历史交易记录了高 LP fee；
- 最终输出仍高于 `amountOutMin`。

函数名、变量名、内部阈值的语义解释都要保留不确定性。

### 2. 12.8% 是结果，不是固定攻击模板

`12.8%` 来自代表交易的 `Swap` event。

它不代表所有毒池都会使用这个费率，也不代表这个数值有某种协议级特殊含义。

更合理的理解是：攻击者在单笔抽取、交易成功率、回滚率、被发现概率之间做了一个选择。这个动机判断属于经济学推断，不是直接证据。

### 3. 低滑点能减少损失，但不能识别恶意路径

低滑点会让异常高费更容易触发回滚，从而限制最坏成交。

但它不能告诉用户：

- 哪个中间池有问题；
- fee 是否在执行时被改写；
- 聚合器是不是被模拟误导；
- 部分路由是否仍走进毒池。

所以用户侧保护只能降低损失，真正的系统性防御在聚合器和钱包基础设施。

### 4. 对抗会继续变化

如果聚合器只补上“不同 gas 模拟”这一项，攻击者还可能切换识别方式，例如：

- 调用者；
- `tx.origin`；
- 中间余额；
- 区块环境；
- 路由拆分比例；
- 多次调用状态。

因此长期防御应当围绕“quote 与 execution 是否一致”建立监控，而不是只针对某个单点条件。

## 扩散分析 / 延展思路

### 1. AMM 路由从价格问题变成执行环境问题

传统聚合器主要优化：

- 深度；
- 价格冲击；
- 手续费；
- gas 成本；
- 多跳路径。

v4 Hook 之后，聚合器还要判断池子程序在不同调用环境下是否一致。

这会让路由系统更像执行安全系统，而不仅是价格搜索引擎。

### 2. 可编程流动性会提高聚合器分层

未来低质量聚合器可能继续只看报价输出，高质量聚合器则会额外维护：

- Hook 风险库；
- 动态费率历史；
- receipt 对账系统；
- toxic pool 黑名单；
- 多环境模拟；
- 异常路线自动回滚或降权。

用户感知到的差别可能不是界面，而是同样一笔 swap，有的钱包会避开毒池，有的钱包会正常成交但少到账。

### 3. 链上收益截图越来越需要行为解释

这类案例也提醒研究者：高 APR、高成交量、低 TVL 不能单独解释为 alpha。

必须追问：

- 交易量从哪里来；
- 路由为何选中该池；
- LP fee 是否稳定；
- 用户还是机器人在承担成本；
- 模拟和执行是否一致；
- 收益窗口是否依赖误导或信息不对称。

否则很容易把攻击窗口误读成可复制策略。

## 一句话结论

这条推文把 Uniswap v4 有毒 Hook 的核心从“高 LP fee”推进到“quote 与 execution 错配”：只要 Hook 能识别聚合器模拟环境并在真实执行中切换费率，交易就可能成功但用户少到账，防御重点必须从用户滑点扩展到聚合器的多环境模拟和 receipt 级对账。
